#include "Twist.h"

namespace Theme {
	Twist::DpiY<float> FontSize(11.f);
	float FieldGroupMargins(0.05f);
	float CurveResolution(1.f);
}

namespace Assets {
	namespace Fonts {
		Twist::Font Body;
	}
}